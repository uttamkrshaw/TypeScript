let firstname: string = "Uttam Kumar"

let lastname: string = "Shaw"

let home: string = `I miss my home`

let phone: number = 9091390251

let doyouhaveenoughmoney: boolean = false

// Array of Number 
let requiredamount: number[] = [1, 2, 3, 4, 5]

// Array of String 
let gadgetsrequired: string[] = ["laptop", "playstation", "monitor"]

// Array of Multiple Types 
let test: (string | number)[] = ["Russians", 32, "DDD"]

// Objects 

type user = { name: string, phone: number, height: number, preferedsize: string }

let uttam: user = {
    name: "uttam kumar shaw",
    phone: 9091390251,
    height: 190,
    preferedsize: "32DDD"
}

// Array of Objects
